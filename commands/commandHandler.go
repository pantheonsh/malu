package commands

import (
	"github.com/bwmarrin/discordgo"
)

// ExecCommand Executa um comando a partir do seu nome.
func ExecCommand(commandName string, args []string, channel *discordgo.Channel, message *discordgo.Message, session *discordgo.Session) {
	return
}
